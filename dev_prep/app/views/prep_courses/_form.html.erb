<%= form_for(@prep_course) do |f| %>
  <% if @prep_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prep_course.errors.count, "error") %> prohibited this prep_course from being saved:</h2>

      <ul>
      <% @prep_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
